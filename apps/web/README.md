# Web App

Next.js + React + TypeScript 前端。当前版本是 IELTS Speaking Live 控制台骨架，后续接入 WebSocket、录音、ASR/TTS、Avatar 和复盘报告。

## 本地运行

```powershell
cd apps/web
pnpm install
pnpm dev
```

## 命令

```powershell
pnpm lint
pnpm typecheck
pnpm build
```

